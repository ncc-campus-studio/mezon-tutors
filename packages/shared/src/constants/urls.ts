/**
 * External / third-party URL constants.
 */

/** Base URL for YouTube embed iframes (append video ID: `${YOUTUBE_EMBED_BASE_URL}/${videoId}`). */
export const YOUTUBE_EMBED_BASE_URL = 'https://www.youtube.com/embed';
