{
  "framework": "nextjs",
  "installCommand": "cd ../.. && corepack enable && corepack prepare yarn@4.11.0 --activate && yarn install",
  "buildCommand": "cd ../.. && yarn workspace @mezon-tutors/db build && yarn workspace @mezon-tutors/shared build && yarn workspace my-app build",
  "build": {
    "env": {
      "NEXT_PUBLIC_API_ENDPOINT": "https://api.mezonly.site/api"
    }
  }
}
